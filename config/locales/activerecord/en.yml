en:
  activerecord:
    attributes:
      coaching_session:
        client_name: client name
        duration: duration
        notes: notes
        starts_at: starts at
        video_embed: video embed
        video_embed_or_url: Code or URL

      user:
        email: email
        name: name

    errors:
      models:
        client_invitation:
          attributes:
            email:
              cannot_match_sender: cannot be your own email

    models:
      admin: admin
      admin_profile: admin profile
      client: client
      client_invitation: client invitation
      client_profile: client profile
      coach: coach
      coaching_session: coaching session
      coach_profile: coach profile
      user: user
