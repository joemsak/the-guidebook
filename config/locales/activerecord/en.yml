en:
  activerecord:
    models:
      admin: admin
      admin_profile: admin profile
      client: client
      client_profile: client profile
      coach: coach
      coach_profile: coach profile
      user: user
    attributes:
      user:
        email: email
        name: name
