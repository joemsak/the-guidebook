<h1><%= t('.title') %></h1>

<p>
  <%= link_to(
    t('actions.manage_thing', thing: t('activerecord.models.coach').pluralize).capitalize,
    admin_coaches_path
  ) %>
</p>

<p>
  <%= link_to(
    t(
      'actions.switch_to',
      thing: "#{t('activerecord.models.coach')} #{t('common.dashboard')}"
    ).capitalize,
    coach_dashboard_path
  ) %>
</p>
